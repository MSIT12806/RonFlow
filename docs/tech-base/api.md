# API

API 是 Application Programming Interface 的縮寫，指的是系統對外提供功能或資料的一組介面。它可以是 HTTP 端點、函式庫方法、訊息契約，或任何讓其他程式與之互動的方式。

在軟體設計裡，API 不只是「可以呼叫的入口」，它同時也在定義系統如何被使用。好的 API 會讓輸入、輸出、錯誤情況與行為邊界都足夠清楚，降低誤用與整合成本。

在 [Outside-In](./outside-in.md) 開發脈絡中，API 常被視為一個很適合往內推進設計的入口。因為它天然站在系統邊界上，能直接反映使用者或其他系統真正需要的互動方式。