# Event Sourcing

Event Sourcing 是一種把事件當成主要事實來源的設計方式。系統不直接把目前狀態當成唯一真相，而是保留每次狀態變化的事件，並在需要時由這些事件重建模型狀態。

它和單純保留 [Domain Event](./domain-event.md) 不完全相同。很多系統會記錄事件，但仍以資料表中的目前狀態作為主要寫入與查詢來源；那不一定算是 Event Sourcing。

Event Sourcing 能提供完整歷史與高度可追溯性，但也會增加重建、版本演進與查詢設計的複雜度，因此不一定適合作為每個專案的預設選擇。