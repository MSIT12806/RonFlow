# AGENTS.md

## RonFlow Agent Entry Point

- Before working on RonFlow project-management tasks, read `docs/RonflowSkills.md`.
- Treat `docs/RonflowSkills.md` as the canonical local bootstrap source for RonFlow AI-agent access.
- After reading `docs/RonflowSkills.md`, follow the RonFlow AI-facing contract returned by the live system.
- If repository files and the live RonFlow system contract disagree, follow the live system contract.
