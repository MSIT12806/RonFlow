# DbMerger src

This directory contains the DbMerger supporting domain implementation.

See `../docs/db-merger-core-spec.md` for the current core spec before adding code.

Current TDD projects:

- `DbMerger.Domain`: domain contract placeholder.
- `DbMerger.Tests`: acceptance tests that define the first implementation slice.
